// Create arrays of names and emails
const names = [
    "Alice",
    "Bob",
    "Charlie",
    "Diana"
];

const emails = [
    "alice@example.com",
    "bob@example.com",
    "charlie@example.com",
    "diana@example.com"
];

// Print all items from both arrays
console.log("Names:");
names.forEach((name) => console.log(name));

console.log("\nEmails:");
emails.forEach((email) => console.log(email));
